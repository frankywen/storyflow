# 进度：2026-03-27 StoryFlow 角色一致性模块

## 已完成

### 1. 图片拼接导出功能 ✅
- 新增 `backend/internal/service/export_service.go` - 导出服务
  - 支持并行下载场景图片
  - 支持图片缩放、拼接成长图
  - 支持 PNG 和 PDF 两种格式导出
- 新增 `backend/internal/api/handler/export_handler.go` - 导出 API
  - POST `/api/v1/export` 接口
- 更新路由配置 `backend/internal/api/router/router.go`

### 2. 视频生成功能 ✅
- 新增 `backend/pkg/ai/video_generator.go` - 视频生成器接口
- 新增 `backend/pkg/ai/kling_video_generator.go` - Kling AI 实现
- 新增 `backend/pkg/ai/runway_video_generator.go` - Runway Gen-3 实现
- 新增 `backend/pkg/ai/mock_video_generator.go` - Mock 实现（测试用）
- 新增 `backend/internal/api/handler/video_handler.go` - 视频 API
  - POST `/api/v1/videos/generate` - 单场景视频生成
  - POST `/api/v1/videos/batch` - 批量视频生成
  - GET `/api/v1/videos/status/:task_id` - 查询任务状态
- 更新 `backend/internal/model/models.go` - Scene 添加 VideoURL 字段
- 更新 `backend/cmd/server/main.go` - 初始化视频生成器

### 3. 前端完善 ✅
- 更新 `frontend/src/App.tsx`:
  - 添加导出按钮（PNG/PDF）到故事详情页
  - 添加视频生成按钮和进度显示
  - 更新工作流程步骤（5步：输入→解析→图片→视频→导出）
  - 设置页面添加保存功能（localStorage 持久化）
- 更新 `frontend/src/types/index.ts` - Scene 添加 video_url 字段
- 更新 `frontend/src/services/api.ts` - 添加 exportApi 和 videoApi

### 4. 角色一致性模块 ✅ (新增)

#### Phase 1: 数据模型扩展
- 更新 `backend/internal/model/models.go`
  - Character 添加 `reference_image_id`, `seed`, `visual_prompt` 字段
- 更新 `frontend/src/types/index.ts`
  - Character 接口添加对应字段

#### Phase 2: 角色一致性服务
- 新增 `backend/internal/service/character_consistency_service.go`
  - `GenerateReferenceImage()` - 为角色生成参考图
  - `EnhanceScenePrompt()` - 增强场景 Prompt
  - `GenerateSceneWithConsistency()` - 带一致性的场景生成
  - `GenerateAllReferenceImages()` - 批量生成所有角色参考图

#### Phase 3: 图片生成器接口扩展
- 更新 `backend/pkg/ai/image_generator.go`
  - 添加 `ImageToImageGenerator` 接口
  - `ImageRequest` 添加 `ReferenceImage`, `RefStrength` 字段

#### Phase 4: API Handler
- 新增 `backend/internal/api/handler/character_handler.go`
  - POST `/api/v1/characters/:id/reference` - 上传参考图
  - DELETE `/api/v1/characters/:id/reference` - 删除参考图
  - POST `/api/v1/characters/:id/regenerate` - 重新生成参考图
  - POST `/api/v1/stories/:id/generate-references` - 批量生成所有角色参考图
- 更新 `backend/internal/repository/story_repository.go`
  - 添加 `GetCharacterByID()` 方法
- 更新 `backend/internal/api/router/router.go`
  - 添加角色相关路由

#### Phase 5: 前端 UI 变更
- 更新 `frontend/src/App.tsx`
  - 添加 `CharacterCard` 组件（展示参考图、上传、重新生成按钮）
  - 故事详情页添加「批量生成参考图」按钮
  - 添加 `handleGenerateAllReferences`, `handleRegenerateReference`, `handleUploadReference` 处理函数
- 更新 `frontend/src/services/api.ts`
  - 添加 `characterApi` 接口

## 进行中

无

## 下一步

- [x] 完整测试流程（测试清单已创建）
- [x] 数据库迁移（通过 GORM AutoMigrate 自动处理）
- [x] 图片批量生成集成角色一致性
- [x] 实际测试（已完成）
  - 创建故事 ✅
  - AI解析 ✅ (阿里云 qwen3.5-plus)
  - 角色参考图生成 ✅ (火山引擎 doubao-seedream)
  - 场景图片批量生成 ✅
  - 导出 PNG ✅
- [x] 修复任务状态更新问题

## 技术栈

| 组件 | 技术 |
|------|------|
| 后端 | Go + Gin + GORM + PostgreSQL |
| 前端 | React 18 + TypeScript + Tailwind CSS |
| AI | Claude API / 火山引擎 / 阿里云 |
| 图片生成 | ComfyUI / 火山引擎 / 通义万相 |
| 视频生成 | Kling / Runway (可选) |
| 图片处理 | github.com/disintegration/imaging |
| PDF生成 | github.com/jung-kurt/gofpdf |

## 环境变量配置

```env
# LLM 配置
LLM_PROVIDER=claude
LLM_API_KEY=your-api-key

# 图片生成配置
IMAGE_PROVIDER=comfyui
IMAGE_BASE_URL=http://localhost:8188

# 视频生成配置 (可选)
VIDEO_PROVIDER=kling  # 或 runway
VIDEO_API_KEY=your-api-key
```

## 备注

- 前端和后端均已成功构建
- zsh 关联数组警告 (`_encode`/`_decode`) 不影响功能，是 shell 兼容性问题